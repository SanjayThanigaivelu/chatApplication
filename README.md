# Chat Application

This is a real-time chat application built using the **MERN stack** (MongoDB, Express.js, React, Node.js).

## Features
- Real-time messaging using WebSockets (Socket.IO)
- User authentication (Login/Signup)
- Send and receive messages instantly
- User-friendly interface
- Supports multiple chat rooms

## Technologies Used
- **Frontend**: React.js, Vite, Socket.IO
- **Backend**: Node.js, Express.js, Socket.IO
- **Database**: MongoDB
- **Authentication**: JWT (JSON Web Tokens)

## Installation

### Prerequisites:
1. Install [Node.js](https://nodejs.org/en/) (v14 or later).
2. Install [MongoDB](https://www.mongodb.com/try/download/community) (if you want to run it locally).

### Steps to Run the Application:

1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/chatapplication.git
